# 近战角色定位与远程武器清理 - 收尾计划

## 背景

用户反馈："坚韧格斗双火的殖民者，射击无火，为何定位为轻甲工人，并且老是主动配备远程武器。不要再考虑近战小人配备远程的事情了，请清理此部分的逻辑。"

上一会话已完成代码改动（3 处），本计划仅处理 README 文档同步与编译验证。

## 当前状态分析

### 代码改动（已完成，已验证）

1. **`Source/AutoEverything/RoleEvaluation/PawnRole.cs` L129-144**
   - `DetectRole` 在技能等级判定之前加入兴趣组合优先判定
   - 近战有火 + 射击无火 → `Brawler`（无论技能等级）
   - 射击有火 + 近战无火 → `Shooter`（无论技能等级）
   - 解决"坚韧+格斗双火但射击无火被定位为 Worker"的问题

2. **`Source/AutoEverything/AutoEquipment/Scoring/Weapon/WeaponTraitScorer.cs` L48-55**
   - 新增 Brawler **角色** Veto 远程武器（`-9000f`）
   - 原仅 `Brawler` 特质 Veto 远程，现扩展到所有 Brawler 角色（含兴趣/技能判定者）
   - 解决"近战定位小人主动配备远程武器"的问题

3. **`Source/AutoEverything/AutoEquipment/Scoring/Weapon/WeaponSkillScorer.cs`**
   - 已删除 `IsDualPassion` 方法
   - 已删除"双修角色远程武器额外 +50"逻辑
   - 仅保留技能等级 × 兴趣乘数的纯技能评分

### README 文档（待同步，3 处过时描述）

经 Grep 确认，README.md 中仍有 3 处引用已删除的逻辑：

| 行号 | 当前内容 | 问题 |
|------|---------|------|
| L83 | `**加分**：+50（参考 WeaponSkillScorer 双修远程偏好分...）` | 引用已删除的"双修远程偏好分" |
| L188 | `\| 3 \| WeaponTraitScorer \| 特质（仅 Brawler 特质否决远程武器，技能型 Brawler 不否决） \|` | 与代码相反：代码已改为 Brawler 角色 Veto |
| L189 | `\| 4 \| WeaponSkillScorer \| 技能等级 × 兴趣乘数...；双修角色（射击+近战均有火）远程武器额外 +50 \|` | 引用已删除的"双修 +50" |

L50 的"双修"行描述角色检测分流（射击≥近战→Shooter，近战>射击→Brawler），与代码 L152/L157/L169/L176 一致，**无需修改**。

## 提议变更

### 变更 1：更新 README.md L188（WeaponTraitScorer 描述）

**文件**：`e:\SteamLibrary\steamapps\common\RimWorld\Mods\AutoEverything\README.md`

**当前**：
```
| 3 | `WeaponTraitScorer` | 特质（仅 `Brawler` 特质否决远程武器，技能型 Brawler 不否决） |
```

**改为**：
```
| 3 | `WeaponTraitScorer` | 角色硬约束：`Brawler` 角色 Veto 远程武器（`-9000f`）；非 Brawler 角色 Veto 近战武器；护盾腰带 Veto 远程；格斗者/敏捷/嗜血/强健特质加分 |
```

**理由**：反映代码现状——Brawler **角色**（而非仅特质）Veto 远程，且该 Scorer 同时承担非 Brawler Veto 近战、护盾腰带 Veto 远程、多项特质加分。

### 变更 2：更新 README.md L189（WeaponSkillScorer 描述）

**文件**：`e:\SteamLibrary\steamapps\common\RimWorld\Mods\AutoEverything\README.md`

**当前**：
```
| 4 | `WeaponSkillScorer` | 技能等级 × 兴趣乘数（无火 1.0 / 单火 1.5 / 双火 2.0）；双修角色（射击+近战均有火）远程武器额外 +50 |
```

**改为**：
```
| 4 | `WeaponSkillScorer` | 技能等级 × 兴趣乘数（无火 1.0 / 单火 1.5 / 双火 2.0） |
```

**理由**：删除已移除的"双修角色远程 +50"描述，与代码现状一致。

### 变更 3：更新 README.md L83（实验服加分描述）

**文件**：`e:\SteamLibrary\steamapps\common\RimWorld\Mods\AutoEverything\README.md`

**当前**：
```
**加分**：+50（参考 `WeaponSkillScorer` 双修远程偏好分，让实验服在同类防具评分中显著领先）。
```

**改为**：
```
**加分**：+50（让实验服在同类防具评分中显著领先，确保研究型殖民者优先穿戴）。
```

**理由**：`WeaponSkillScorer` 已无"双修远程偏好分"，原引用对象不存在。保留 +50 加分描述（`ApparelLabCoatScorer` 逻辑未变），改为说明加分意图。

### 变更 4：编译验证

**命令**：`make check`

**预期**：0 警告 0 错误（代码未改动，仅文档同步）。

## 假设与决策

1. **L50 双修行保留**：双修（射击+近战均有火）会跳过兴趣优先判定（L134/L140 均要求"另一方无火"），进入技能等级判定。L50 描述"射击≥近战→Shooter，近战>射击→Brawler"与代码一致，准确无误。

2. **不修改代码**：上一会话的 3 处代码改动已通过 Read 验证到位，本计划仅做文档同步。

3. **不更新项目记忆**：本次改动属于"近战角色定位与远程武器清理"的收尾，核心记忆已在上一会话沉淀；本收尾不引入新约束。

## 验证步骤

1. 应用变更 1-3（README.md 三处编辑）
2. 运行 `make check`，确认 0 警告 0 错误
3. Grep 复查 README 无残留"双修远程偏好"/"技能型 Brawler 不否决"等过时描述
